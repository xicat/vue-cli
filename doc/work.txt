工作计划 


home about 模块 关闭菜单  ；  其他模块 显示菜单
 
先从日记模块开始

 先完成 mock 
 设置 vue.config.js


 2019-04-11 
 今日问题 
 diary 页面直接打开时  mounted加载数据 不能被mock 拦截  而切换路由就可以被拦截。
 定位半天发现 才发现 mock 定义时 加载模块用 import ('./modules/diary')
 这是一个延时加载。改为 import './modules/diary'
 

功能开发先押后。现在弄代码检测

2019-04-15
1 完成  githook pre-commit 标准化代码 
2 完成 mock.js 假数据
3 完成  build代码 提交到服务器 
4 完成 element-ui table封装 


.git/hooks/pre-commit 文件  
#!/bin/sh
#
# An example hook script to verify what is about to be committed.
# Called by "git commit" with no arguments.  The hook should
# exit with non-zero status after issuing an appropriate message if
# it wants to stop the commit.
#
# To enable this hook, rename this file to "pre-commit".

STAGE_FILES=$(git diff --cached --name-only --diff-filter=ACM -- '*.vue' '*.js')
if test ${#STAGE_FILES} -gt 0
then
    echo '开始检查'


    for FILE in $STAGE_FILES
    do
        npm run lint $FILE
		git add $FILE
        if [[ "$?" == 1 ]]; then
      PASS=false
    fi
  done

  if ! $PASS; then
      echo "检查没通过！"
      exit 1
  else
      echo "检查完毕"
  fi

else
    echo '没有文件需要检查'
fi

exit 0



2019 4 17
着手弹框的框架。

下一步目标  koa2 框架在服务器搭建 
先 学习es6




